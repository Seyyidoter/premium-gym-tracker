import { PhasePlaceholder } from '@/components/PhasePlaceholder';
import { Screen } from '@/components/Screen';

export default function RoutinesPlaceholderScreen() {
  return (
    <Screen title="Routines">
      <PhasePlaceholder body="Routine creation and assignment start in Phase 2. Repository foundations are already available." />
    </Screen>
  );
}
